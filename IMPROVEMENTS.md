# 인증 보완 작업 및 적용 영향

## 후속 작업: 공지 저장·접속 분석·조회 응답 순서 보완

운영 반영은 사용자가 Supabase SQL Editor에서 직접 실행하기로 했다. 현재 아래 SQL은 로컬 임시 DB에서 검증한 상태이며 운영 적용 결과는 아직 받지 않았다. 앱 배포도 하지 않았다.

### 먼저 실행할 순서

1. `supabase/pin_null_guard_migration.sql`: 기존 PIN 관련 SQL 이후 적용. 이미 적용했다면 재실행할 필요는 없다.
2. `supabase/notice_atomic_save_migration.sql`: 새 공지 저장 화면 배포 전에 실행한다.
3. `supabase/analytics_summary_migration.sql`: 새 접속 분석 화면 배포 전에 실행한다.
4. `supabase/security_audit_readonly.sql`: 읽기 전용 점검 결과 JSON을 전달한다. 실제 테이블 구조, 정책, 익명/인증 역할 권한, 외래키, 인덱스, PIN 패치 여부를 확인한다. 학생 이름·전화번호·PIN 해시·키를 조회하지 않는다.

**2·3번을 적용하지 않고 새 앱을 배포하면 공지 저장과 접속 분석이 오류를 표시한다.** 안전하지 않은 옛 저장 방식이나 누락될 수 있는 옛 집계 방식으로 자동 복귀하지 않는다. SQL부터 적용하면 기존 앱이 사용 중일 때도 함수 추가만 수행하므로 기존 호출 방식은 유지된다. 각 SQL 파일은 전체 실행하며 반복 적용할 수 있다.

### 공지 저장 변경과 영향

- 본문 저장 → 대상 삭제 → 대상 추가의 여러 요청을 `save_notice_with_targets` 단일 DB 트랜잭션으로 전환했다.
- 실패 시 기존 본문·대상 목록이 함께 유지된다. 신규 등록 중 실패한 경우 공지 자체도 남지 않는다.
- 선택 대상 0명, 존재하지 않는 학생, 잘못된 대상 모드는 거부한다. 전체 공개는 전체 모드로 명시해야 한다.
- 서버에서 승인된 관리자 여부와 작성자 ID를 확인한다. 편집은 기존 공지 행을 잠그고 처리하며 생성 시각·작성자는 유지한다.
- 신규 공지 시각은 기존 날짜만 저장하던 값 대신 DB의 실제 저장 시각을 사용한다. 기존 공지 시각은 바꾸지 않는다.
- 실패 시 모달·입력 내용을 유지하며 푸시는 저장 성공 후 신규 공지에만 요청한다. 공지 삭제도 DB 오류를 확인한다.
- 기존 직접 쓰기 권한과 읽기 RLS는 이번에 바꾸지 않았다. 오래 열린 이전 버전 탭에서는 기존 저장 경로가 남을 수 있으므로 배포 후 새로고침해야 한다.
- 학생 삭제 등으로 대상 관계가 나중에 모두 사라지는 경우의 공개 의미, 대상 조회 실패, 익명 본문 접근 문제는 별도의 공개 범위 모델/RLS 보완이 필요하다. 이번 원자화로 모든 공지 접근 문제가 해결된 것은 아니다.

### 접속 분석 변경과 영향

- 원본 방문/상담 행을 브라우저에 내려받아 계산하던 방식을 관리자 전용 DB 집계 RPC로 전환했다.
- 반환 한도(예: 1,000행)에 의한 집계 누락을 제거한다. 브라우저에는 요약 수치와 일별 14개·월별 12개 데이터만 반환한다.
- 방문·상담은 서버 기준 최근 366일, 날짜·월 구분은 한국 시간이다. 미래 시각으로 잘못 기록된 행은 제외한다.
- 방문 기록이 1,000건을 넘었던 경우 기존 표시보다 집계 수치가 증가할 수 있다. 데이터가 새로 생긴 것이 아니라 누락된 행을 포함하는 변화다.
- 서버의 실제 집계 실행 시간은 운영 측정 전이다. 전송량은 원본 행 수에 비례하지 않지만 DB는 기간 내 행들을 집계해야 한다.
- 월말/윤년/시간대 테스트를 DB 집계 테스트로 옮겼으며, 더 이상 쓰지 않는 클라이언트 월 목록 계산 함수를 제거했다.
- RPC 권한/조회 실패를 0건 통계로 표시하지 않고 오류 상태로 표시한다.

### 학부모·학생 수업기록 조회 변경과 영향

- 자녀/학생 ID 변경과 화면 이탈 시 이전 조회를 취소한다. 취소 이후 도착하는 응답도 검사해 상태 갱신을 막는다.
- 전환 시 이전 기록·반 이름·학부모 댓글 표시·수정 안내를 초기화한다. 입력한 댓글 초안은 삭제하지 않는다.
- 취소된 초기 조회에서 읽음 기록을 새로 보내지 않으며, 이미 전송한 읽음 기록의 늦은 응답은 새 화면을 갱신하지 않는다.
- 사용자가 직접 누른 댓글 전송 등 쓰기는 취소하거나 자동 재시도하지 않는다.
- 교직원 `/stats`의 학생 선택 조회는 이번 변경 대상에 포함되지 않는다.

### 검증 및 다음 단계

- PIN/공지/접속 분석 SQL은 임시 PostgreSQL(PGlite)에서 반복 적용과 권한을 검증했다. 운영에 실행한 결과는 아니다.
- 공지 대상 삽입 단계의 강제 실패·기존 데이터 롤백·선택 0명 거부·관리자 권한을 테스트했다.
- 방문 1,502건/상담 1,201건, 한국 시간 날짜 경계, 3월 31일, 윤년, DB 시간대 변경을 검증했다.
- 실제 학부모·학생 페이지 함수를 실행하면서 늦은 기록/반 조회 응답을 전달해 취소 후 덮어쓰기·읽음 쓰기가 발생하지 않는지 검증했다.
- 타입 검사·프로덕션 빌드·전체 테스트 결과는 작업 종료 시 최종 보고한다. 화면 HTTP 200은 실제 운영 DB 기능 검증을 의미하지 않는다.
- 학부모·학생 RLS, 쿠폰 발급 권한, FCM 소유권, 수업기록 트랜잭션은 운영 점검 결과 확인 후 진행한다. 높은 우선순위를 낮춘 것이 아니라, 실제 정책·스키마를 모르는 상태에서 사용 중인 기능을 중단시키지 않기 위한 의존성이다.

2026-09-25. 기존 Claude 정리 및 이전 날짜 수정에 추가한 작업이다.

## 적용 상태

- 로컬 코드 수정과 검증 완료. Git 커밋/푸시 및 운영 배포는 하지 않았다.
- 운영 Supabase에는 SQL을 실행하지 않았다. PIN 보완은 아래 SQL을 적용해야 효력이 생긴다.
- 기존 DB 데이터, PIN 해시, RLS 정책을 변경하지 않았다.

## 관리 API 인증

대상: `/api/teachers`의 GET/POST/DELETE, `/api/attendance/approve`와 `/api/attendance/update`의 POST.

기존에는 요청의 `requesterId`/`approvedBy`를 신뢰하거나, `approvedBy`를 생략하면 검사를 건너뛰었다. 이제 `Authorization: Bearer ...` 토큰을 Supabase Auth 서버에서 검증하고, 해당 사용자의 teachers 행이 `role=admin`, `approved=true`인지 확인한다. 승인자 ID도 이 검증 결과로 기록한다.

- 승인된 관리자의 기존 근태 승인·취소·일괄 승인·시간 수정 흐름을 유지한다.
- 로그인하지 않은 요청과 잘못된 토큰은 401, 관리자 이외 또는 미승인 사용자는 403으로 거부한다.
- 권한 조회 장애는 503으로 처리하며 쓰기 작업을 진행하지 않는다.
- 승인 취소 시 승인자와 승인 시각을 비운다. 수정 API에서도 같은 규칙을 적용한다.
- 교직원 화면의 근태 호출은 `staffFetch`를 사용해 기존 Supabase 세션을 전달한다. 새 로그인 방식이나 쿠키 설정은 필요하지 않다.
- 네트워크 오류 시 쓰기를 자동 재시도하지 않는다. 처리 여부가 불확실할 수 있어 결과 확인 안내를 표시한다.
- API마다 토큰 검증과 프로필 조회가 추가된다. 일괄 승인은 기존처럼 건별 요청이므로 승인 건수만큼 인증 비용이 추가된다. 실제 운영 지연 시간은 측정하지 않았다.
- 프런트엔드와 API를 같은 배포로 적용해야 한다. 배포 전에 열린 탭이나 기존 외부 호출자는 토큰이 없으면 거부된다. 탭을 새로고침하고 필요 시 다시 로그인해야 한다.

## 교직원 계정 삭제

브라우저에서 관리자 전용 Auth API를 호출하고 오류를 무시한 뒤 프로필만 삭제하던 흐름을 인증된 서버 DELETE API로 연결했다.

- Auth 삭제 실패 시 성공 알림을 띄우지 않으며 프로필 삭제도 진행하지 않는다.
- 대상이 teachers에 있는지 확인한다. 현재 로그인한 본인 계정 삭제는 거부한다.
- Auth 삭제 성공 후 프로필을 정리한다. DB에 FK CASCADE가 있으면 이미 정리된 행에 대한 삭제는 무해하다.
- Auth와 프로필 삭제는 서로 다른 작업이므로 하나의 트랜잭션이 아니다. Auth 삭제 후 프로필 정리 실패 시 부분 실패 메시지를 표시하며 관리자의 별도 정리가 필요하다.
- 실제 계정 삭제는 테스트용 모의 클라이언트로 검증했다. 운영 계정을 삭제하지 않았으며, 운영 FK에 연결된 근태 등 데이터의 연쇄 삭제 범위는 확인하지 않았다. 이 변경은 기존 서버 Auth 삭제 기능을 화면에 정상 연결한 것이므로 해당 제약은 운영 배포 전에 확인해야 한다.

## PIN NULL 우회 차단

적용 파일: `supabase/pin_null_guard_migration.sql`.

- 부모/학생의 검증 함수 및 PIN 변경 함수 총 4개를 교체한다.
- NULL, 빈 값, 4자리 숫자가 아닌 값 및 일치하지 않는 PIN을 거부한다. 해시 비교에 NULL 안전 비교를 사용한다.
- 기존 PIN 해시, 정상 로그인 반환 형식, 자녀 목록, 기본 PIN 0000, 정상 PIN 변경을 유지한다.
- 함수 search_path를 고정하고 PUBLIC 실행 권한 대신 anon/authenticated 역할에 실행 권한을 명시한다.
- 테이블 및 데이터 변경은 없다. 전체 SQL은 트랜잭션이며 반복 적용 가능하다.
- 기존 `pin_hash_migration.sql` 및 `pin_verify_ambiguous_fix.sql` 이후 SQL Editor에서 파일 전체를 실행한다. 과거 PIN SQL을 이 패치 뒤에 다시 실행하면 취약한 정의로 되돌아갈 수 있다.
- 기존 앱과 인자/반환 형식이 같아 앱 배포보다 먼저 적용해도 된다. 적용 후 테스트 계정으로 정상 PIN 로그인·잘못된 PIN 거부·PIN 변경·기본 PIN 설정 흐름을 확인한다.
- 로컬 테스트는 실제 pgcrypto를 사용하는 임시 PGlite에서 anon/authenticated 역할로 실행했다. 운영 함수·권한과의 비교는 하지 않았다.

## 검증 결과

- `npx tsc --noEmit`: 통과.
- `npm run build`: 통과.
- `npm test`: 48개 통과(기존 42개 + 인증/API 5개 + PIN DB 통합 1개).
- 신규 인증 헬퍼, 수정 API, 신규 테스트 대상 ESLint: 통과. 전체 프로젝트의 기존 훅 lint 문제를 해결했다는 의미는 아니다.
- 개발 서버: 보호 API 5개 동작 모두 토큰 없는 요청에 401. teachers/attendance/parent records/student records 화면 모두 HTTP 200.
- 정상 관리자 토큰·역할·근태 저장·계정 삭제 실패/성공은 모의 클라이언트 테스트로 확인했다. 실계정 로그인 후 브라우저 E2E 및 운영 쓰기 검증은 하지 않았다.

## 남은 작업과 이유

1. 학부모·학생 RLS 소유권: 기존 익명 접근과 브라우저 신원 저장에 의존하므로 정책만 닫으면 사용 중인 화면이 중단된다. 서버가 검증 가능한 공통 신원과 모든 호출의 이관이 선행되어야 한다.
2. 일반 PIN 로그인 횟수 제한: 이번 패치는 NULL 우회만 차단한다. 반복 시도 제한은 별도다. 기존 오류 반환 방식에서 실패 카운트가 트랜잭션 롤백되지 않도록 설계해야 한다.
3. FCM 토큰 소유권·푸시·키오스크 인증: 학부모/학생 공통 신원 및 예약 작업/기기별 인증과 함께 변경해야 한다.
4. 공지 대상·수업기록·쿠폰 저장 트랜잭션: 운영 스키마·정책 확인 후 RPC 적용과 앱 전환을 묶어 진행해야 한다. 이 작업에서 기존 저장 경로는 변경하지 않았다.
5. 접속 분석 서버 집계·목록 페이지 조회: 데이터 반환 한도에 의한 누락은 여전히 검토 대상이다. 운영 집계 쿼리/인덱스와 전체 기간 조회 요구를 확인해 별도 반영해야 한다.

이번 변경으로 모든 접근 권한 문제가 해결된 것은 아니다. 특히 브라우저의 Supabase 직접 접근은 이번 관리 API 검사를 거치지 않으므로 별도의 RLS 점검이 필요하다.
